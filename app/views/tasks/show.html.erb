<p id="notice"><%= notice %></p>

<h1>Task</h1>

<p>
  <strong>Name:</strong>
  <%= @task.name %>
</p>

<p>
  <strong>Description:</strong>
  <%= @task.description %>
</p>

<p>
  <strong>List:</strong>
  <%= @task.list %>
</p>

<%= link_to 'Edit', edit_list_task_path(@list, @task), remote: true %> |
<%= link_to 'Back', list_tasks_path(@list), remote: true %>
